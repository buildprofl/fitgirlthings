╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║              FIT GIRL THINGS — CLAUDE CODE UPLOAD INSTRUCTIONS              ║
║                                                                              ║
║                        5 MINUTES TO LIVE SITE                               ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════════════════════════

✅ WHAT YOU HAVE:

All source files are in /mnt/user-data/outputs/:

  ✓ App.jsx (18 KB)
  ✓ styles.css (12 KB)  
  ✓ main.jsx
  ✓ package.json
  ✓ vite.config.js
  ✓ index.html
  ✓ netlify.toml
  ✓ gitignore (rename to .gitignore)
  ✓ UPLOAD_TO_CLAUDE_CODE.md (full instructions)
  ✓ CLAUDE_CODE_SETUP.txt (this file)

═══════════════════════════════════════════════════════════════════════════════

🚀 STEP 1: OPEN CLAUDE CODE

Go to: https://claude.ai or launch Claude Code

═══════════════════════════════════════════════════════════════════════════════

🚀 STEP 2: CREATE NEW PROJECT

1. Click "Create new project"
2. Name it: fitgirlthings
3. Select: React + JavaScript
4. Click "Create"

═══════════════════════════════════════════════════════════════════════════════

🚀 STEP 3: UPLOAD FILES

**Root level files (drag into root directory):**
  □ package.json
  □ vite.config.js
  □ index.html
  □ netlify.toml
  □ gitignore (rename to .gitignore after upload)

**In src/ folder:**
  □ App.jsx
  □ main.jsx
  □ styles.css

**Directory structure should look like:**
  fitgirlthings/
  ├── package.json
  ├── vite.config.js
  ├── index.html
  ├── netlify.toml
  ├── .gitignore
  └── src/
      ├── App.jsx
      ├── main.jsx
      └── styles.css

═══════════════════════════════════════════════════════════════════════════════

🚀 STEP 4: OPEN TERMINAL IN CLAUDE CODE

1. Press: Ctrl+` (backtick) to open terminal
2. Run: npm install
3. Wait for completion (~30 seconds)
4. Run: npm run build
5. Verify: dist/ folder appears with files ✅

═══════════════════════════════════════════════════════════════════════════════

🚀 STEP 5: CONNECT TO GITHUB

1. In Claude Code, click "Connect to GitHub"
2. Authorize Claude Code to access buildprofl/fitgirlthings
3. Confirm connection

═══════════════════════════════════════════════════════════════════════════════

🚀 STEP 6: PUSH TO GITHUB

In Claude Code terminal, run:

  git add .
  git commit -m "Add React source code - complete editorial site"
  git push origin main

Wait for push to complete (~10 seconds)

═══════════════════════════════════════════════════════════════════════════════

✅ AUTOMATIC DEPLOYMENT (2-3 MINUTES):

After you push:

1. GitHub receives code ✅
2. Webhook triggers → Netlify starts build
3. Netlify runs: npm install + npm run build
4. Netlify deploys dist/ to CDN
5. Site goes LIVE at: https://fitgirlthings.netlify.app ✅

═══════════════════════════════════════════════════════════════════════════════

🎯 VERIFY DEPLOYMENT:

1. Check Netlify: https://app.netlify.com/projects/fitgirlthings/deploys
   Look for: Green checkmark ✅ "Published"

2. Visit your site: https://fitgirlthings.netlify.app
   You should see:
   - Hero section with newsletter CTA
   - 3 feature cards
   - About section
   - 6 editorial articles
   - Events section
   - Partner section
   - Footer with links

═══════════════════════════════════════════════════════════════════════════════

📋 FILE SIZES & WHAT THEY DO:

package.json (0.4 KB)
  → Lists all npm dependencies (React, Vite, etc.)
  → Defines build commands

vite.config.js (0.3 KB)
  → Vite build configuration
  → Tells Vite to use React plugin

index.html (1.4 KB)
  → Entry HTML file
  → Links fonts (Cormorant Garamond, Inter)
  → Mounts React app to #root div

netlify.toml (0.2 KB)
  → Netlify build configuration
  → Tells Netlify: run "npm run build" → deploy "dist/"

.gitignore (0.2 KB)
  → Tells Git to ignore node_modules, dist, etc.

src/main.jsx (0.2 KB)
  → React entry point
  → Mounts App component to DOM

src/App.jsx (18 KB)
  → All components, pages, content
  → Newsletter form
  → Event listings
  → Article cards
  → All sections

src/styles.css (12 KB)
  → Complete styling
  → Color scheme (pink/blush)
  → Typography
  → Responsive grid
  → Mobile menu

TOTAL: ~33 KB of source code
BUILD OUTPUT: 50 KB gzipped (170 KB uncompressed)

═══════════════════════════════════════════════════════════════════════════════

⚡ TIMELINE:

  0 min   You upload files to Claude Code
  +1 min  npm install completes
  +2 min  npm run build completes
  +3 min  Connect to GitHub
  +4 min  git push completes
  +5 min  GitHub receives code
  +6 min  Netlify starts building
  +8 min  Netlify finishes build
  ────────────────────────────────
  +8 min  ✅ SITE IS LIVE!

═══════════════════════════════════════════════════════════════════════════════

🆘 TROUBLESHOOTING:

Build fails?
  → Check terminal errors
  → Make sure all 8 files are in correct folders
  → Verify .gitignore is named correctly (starts with .)

Git push fails?
  → Make sure GitHub is connected in Claude Code
  → Check git config: name and email
  → Verify you have write access to fitgirlthings repo

Site is blank after deployment?
  → Wait 2 minutes (Netlify CDN propagation)
  → Hard refresh browser: Ctrl+Shift+Del, then refresh
  → Check Netlify logs for build errors
  → Verify netlify.toml has correct build command

═══════════════════════════════════════════════════════════════════════════════

✨ THAT'S IT!

All 8 files are in /mnt/user-data/outputs/

Download them → Upload to Claude Code → Push to GitHub → Done! 

Your site will be LIVE in 10 minutes.

═══════════════════════════════════════════════════════════════════════════════
