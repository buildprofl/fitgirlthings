---
import NewsletterForm from '~/components/react/NewsletterForm.jsx'
---

<section id="newsletter" class="newsletter section blush">
  <div class="container newsletter-wrap">
    <div class="reveal">
      <span class="eyebrow">Fit Girl Insider</span>
      <h2>Get it first, while it's still hot.</h2>
      <p>
        Studio openings before they're public, industry news, wellness finds, and trainer
        spotlights — straight to your inbox, before anyone else sees it.
      </p>
      <ul class="perks">
        <li>Early access to what's Spotted around Miami</li>
        <li>Product finds and gym-bag essentials, curated</li>
        <li>Zero spam — just the tea, when it's actually worth sharing</li>
      </ul>
    </div>
    <div class="newsletter-panel reveal">
      <NewsletterForm client:only="react" source="home-insider" />
      <p class="micro-copy">✦ Join the girls who see it first.</p>
    </div>
  </div>
</section>

<style>
  .newsletter-wrap {
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap: 2rem;
    align-items: center;
  }

  .newsletter-wrap h2 {
    font-family: var(--font-serif);
    font-size: clamp(2rem, 3.6vw, 3.4rem);
    line-height: 0.98;
    letter-spacing: -0.03em;
    margin: 0.5rem 0 1rem;
  }

  .newsletter-wrap p {
    color: var(--muted);
    line-height: var(--leading-relaxed);
    max-width: 46ch;
  }

  .perks {
    list-style: none;
    padding: 0;
    margin: 1.25rem 0 0;
    display: grid;
    gap: 0.5rem;
  }

  .perks li {
    color: var(--text);
    font-weight: 500;
    padding-left: 1.5rem;
    position: relative;
  }

  .perks li::before {
    content: '✦';
    position: absolute;
    left: 0;
    color: var(--accent-strong);
  }

  .newsletter-panel {
    padding: 1.75rem;
    border-radius: var(--radius-xl);
    background: color-mix(in oklab, var(--surface) 90%, transparent);
    border: 1px solid var(--line);
    box-shadow: var(--shadow);
  }

  .micro-copy {
    color: var(--muted);
    font-size: 0.9rem;
    margin: 0.85rem 0 0;
    text-align: center;
  }

  @media (max-width: 900px) {
    .newsletter-wrap { grid-template-columns: 1fr; }
  }
</style>
